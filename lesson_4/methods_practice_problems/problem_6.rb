['ant', 'bear', 'caterpillar'].pop.size

# => pop removes and returns trailing elements without an argument
# => size returns the count of elements in self
# => pop returns 'caterpillar' and size is called on that value
# => 11