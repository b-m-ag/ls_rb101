arr = [1, 2, 3, 4, 5]
arr.take(2)

# docs say: Returns a new Array containing the first n element of self
# where n is a non-negative Integer; does not modify self

# This returns a new array, which is not destructive

# the above problem would return the first two elements of arr:
# => [1, 2]